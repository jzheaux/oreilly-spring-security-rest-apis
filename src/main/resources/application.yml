logging:
  level:
    org.springframework.security: TRACE

spring:
  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://idp:8083/oauth2/jwks